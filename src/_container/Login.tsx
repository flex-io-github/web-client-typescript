// import LoginPage from '../LoginPage/LoginPage';
// import { connect } from 'react-redux';

export interface state {
    username: string;
    password: string;
}

// export function mapStateToProps( {username, password} : state) {
//     return {
//         username,
//         password
//     };
// }

// export default connect(mapStateToProps)(LoginPage);